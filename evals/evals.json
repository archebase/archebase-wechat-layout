{
  "skill_name": "archebase-wechat-layout",
  "evals": [
    {
      "id": 1,
      "prompt": "Use InkPost to lay out an ArcheBase technical WeChat article. Keep the visual style consistent with the VI Guide and tell me what must be checked before copying it into WeChat.",
      "expected_output": "The agent loads the VI dependency and the InkPost workflow, uses the canonical CSS source, validates CSS and the InkPost scanner, inspects the rendered article and returns a release verdict.",
      "files": [],
      "expectations": [
        "The response distinguishes brand rules from InkPost/WeChat implementation rules.",
        "The response uses the explicit VI colors without converting the p.28 labels into component quotas.",
        "The response requires CSS validation, InkPost scanner results and rendered visual inspection.",
        "The response reports unresolved claims, rights, naming, font and Logo approvals."
      ]
    },
    {
      "id": 2,
      "prompt": "The built-in ArcheBase theme in our InkPost checkout was edited. Determine whether it is safe to release and whether the shared WeChat skill and InkPost preset still agree.",
      "expected_output": "The agent runs the canonical CSS validator and preset parity checker, identifies any mismatch and blocks release until sources agree.",
      "files": [],
      "expectations": [
        "The response treats the skill CSS as canonical and the InkPost file as a runtime wrapper.",
        "The response runs or requests a deterministic parity check rather than comparing names only.",
        "The response does not inspect or commit the complete local InkPost user store."
      ]
    },
    {
      "id": 3,
      "prompt": "Given this Markdown article and the canonical ArcheBase WeChat CSS, call the available InkPost render capability and return the rendered HTML that can be pasted into WeChat. If the endpoint is unavailable, explain the UI fallback.",
      "expected_output": "The agent calls or describes calling InkPost /api/render with markdown and CSS, returns the RenderResult html as the primary deliverable, reports warnings and uses the UI only as fallback.",
      "files": [],
      "expectations": [
        "The response treats InkPost as the rendering capability rather than merely instructing the user to open it.",
        "The response uses the canonical CSS and the /api/render request shape.",
        "The response returns rendered HTML as the primary output and preserves InkPost warnings/stats.",
        "The response does not modify InkPost source or invent a second renderer."
      ]
    },
    {
      "id": 4,
      "prompt": "Given a Markdown WeChat article, produce the final HTML directly without requiring InkPost to be installed or running. Use the bundled ArcheBase WeChat renderer and CSS, report warnings, and save the result as HTML for pasting into WeChat.",
      "expected_output": "The agent runs the bundled renderer command, uses canonical CSS, writes InkPost-compatible #nice HTML and reports render statistics without calling a remote endpoint.",
      "files": [],
      "expectations": [
        "The response treats the WeChat layout skill as self-contained and does not require a running InkPost service.",
        "The response runs the bundled renderer command with the supplied Markdown and canonical CSS.",
        "The response returns or saves rendered #nice HTML and reports warnings and statistics.",
        "The response does not modify the separate InkPost application."
      ]
    }
  ]
}
